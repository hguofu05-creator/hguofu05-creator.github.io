# 2026-07-27

## 从零搭建个人摄影作品集网站（Astro 5 + Tailwind + GitHub Pages）

- 技术栈：Astro 5 + Tailwind CSS v3（`@astrojs/tailwind` 集成）+ TypeScript。纯静态，无后端/DB/CMS。
- 配色：影视飓风风格，定义在 `tailwind.config.mjs`（oklch: bg 0.15/0/0, bg-2 0.22, fg 0.95, muted 0.65 0.01 260, accent 0.75 0.15 240）。
- 5 个页面：`src/pages/` 下 index(首页Hero+瀑布流) / gallery(筛选+网格) / photo/[slug](详情+灯箱+上下张) / about / contact / 404。
- 照片源：桌面 `C:\Users\ocean\Desktop\作品集`（84 张手机 JPG，含 MVIMG 动态照片、mmexport 微信导出）。
- 图片管线：`scripts/process-images.mjs` 用 sharp 生成 400/800/1600 WebP 到 `public/images/photos/`，exifr 抽 EXIF，写 `src/data/photos.json`。
  - 手机 EXIF 缺 Make/Model/FNumber/ExposureTime/FocalLength；日期从文件名解析（IMG_YYYYMMDD / mmexport<epoch13>），ISO 用 ISOSpeed，光圈用 MaxApertureValue(APEX)。
  - 已实际运行：生成 252 个 WebP（23MB，原图 313MB），photos.json 84 条。
- `npm run build` 通过（89 页），`npm run preview` 各路由 200。
- 部署：`.github/workflows/deploy.yml` 走 GitHub Actions；推荐仓库命名 `<用户名>.github.io`（免 base 配置）。
- 注意事项：用户是编程新手，下一步需手动编辑 `src/data/photos.json` 补 title/category/story/location，并在 `src/consts.ts` 改名字/邮箱/社交链接；关于页头像放 `public/images/about.webp`。

## 元数据补全 + 分类纠错 + 公网部署（续上午）

- 4 个 `scripts/analysis-partN.json`（4 个子代理各 21 张）已合并进 `src/data/photos.json`（84 条全部匹配，无缺失）。
- 按用户规则纠错：
  - 人文(people) 中 10 张无真人（烟花/广州塔/郑裕彤楼/牌坊/铁塔/电塔/街角灯/晚霞/电线杆/远塔）→ 改「城市」。最终 people=13（均含真人：身影/手心的秋叶/小男孩/少年/一家人/双背影/独行的人/两个身影/桥下三人/小女孩/巨龙人潮/求婚/独坐看山人）。
  - 建筑/基建错放森林或山岳（飞檐/亭台/牌坊/铁塔/工地/古塔/楼头/路牌/电线杆/城市天际线等 12 张）→「城市」。
  - 纯云天错放水域/森林（飞机穿云/机翼云海/云隙光/弯月晚霞/暮色云天 5 张）→「山岳」(landscape 桶，仍属自然)。
  - 分布：城市22 / 森林22 / 山岳15 / 人文13 / 水域12。
- EXIF 仅保留 ISO + 光圈（`src/lib/photos.ts` Exif 接口 + 详情页表格已简化；photos.json 也清洗掉 camera/lens/focalLength/shutter）。
- 站点信息已改（`src/consts.ts`）：name=Leo，email=15889902802@163.com，社交=抖音/B站/小红书官网；头像 `public/images/about.webp`（占位，用户可随时覆盖）。
- `npm run build` 通过（89 页）。本地预览仅 localhost，不满足用户「任何网络可访问」诉求。
- **公网部署**：用 `workbuddy_cloudstudio_deploy` 把 `dist/` 部署到 CloudStudio 沙箱，已验证首页/图片/各路由均 200。链接：https://55baee46f49b4a6a9a4d65e048562749.app.codebuddy.work （沙箱链接，可能有过期/生命周期，非永久）。
- git 已 `init` 并首次 commit（源码 + 84 张 WebP；dist/node_modules/.astro 已被 .gitignore 排除）。GitHub Pages 永久方案待用户给 GitHub 用户名（或连接 GitHub）后 `git remote add` + push main，Actions 自动部署（deploy.yml 就绪）。
- 说明：本环境 Read 图片被拦截，无法逐张肉眼校验分类；纠错基于子代理标题/描述的逻辑判断（是否含真人/建筑），已在最终回复向用户透明说明，并提示纯云天类照片的子分类可能需用户微调。
